services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: omerta
      POSTGRES_PASSWORD: omerta_dev
      POSTGRES_DB: omerta
    ports:
      - "5432:5432"
    volumes:
      - omerta_pgdata:/var/lib/postgresql/data

volumes:
  omerta_pgdata:
